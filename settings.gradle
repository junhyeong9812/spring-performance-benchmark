pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'spring-performance-benchmark'

include 'common'
include 'mvc-app'
include 'webflux-app'